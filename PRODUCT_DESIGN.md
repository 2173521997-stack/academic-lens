# 产品设计方案：Academic Lens（学术透镜）

**定位**：像苹果原生应用一样安静、快速、聪明的英文阅读伴侣 —— 课件、论文、图片、扫描件，什么格式都能译。

---

## 1. 核心用户场景

| 场景 | 痛点 | 解决 |
|------|------|------|
| 读英文课件/论文 | 生词打断阅读流 | 划词浮窗，300ms 出结果 |
| 长难句 | 句子结构看不懂 | 双语对照 + 分段讲解 |
| 公式 | 符号含义不明 | 选中公式，AI 大白话讲推导 |
| 图片/PPT 截图 | 纯图无文字可复制 | 截图/图片翻译（OCR） |
| 扫描版论文 | 复制不了、通篇啃不动 | 上传文件 → 摘要 + 全文翻译 |
| 术语积累 | 记不住反复出现的术语 | 自动生词本 + 术语卡片 |

---

## 2. 功能架构（五个层次）

```
① 划词翻译     全局取词 → 毛玻璃浮窗
② 截图/图片翻译 框选截图 → OCR → 原文译文逐行对照
③ 文件上传翻译  拖入 PDF/Word/PPT/图片 → 结构保留翻译 + 一键总结
④ AI 学术助手  带上下文聊天、@引用段落、/快捷指令、基于文件问答
⑤ 底座         生词本 · 历史 · 术语库 · 设置
```

### 2.1 划词翻译（核心入口）

- 全局快捷键（`Ctrl+Shift+T`）→ 毛玻璃小浮窗
- 浮窗内容分层：**单词** → 音标 + 释义 + 例句 + 领域术语徽章；**短语/短句** → 整句翻译 + "讲解"
- OCR 兜底：无法取词的场景（扫描版 PDF）自动截屏识别
- 浮窗操作：`[翻译] [讲解] [收藏]` 三个胶囊小按钮，轻触外部即消失

### 2.2 截图与图片翻译

- 全局截图（快捷键框选任意区域）→ 自动 OCR → 结果面板：**左侧原图缩略 + 识别原文，右侧译文，逐行对齐**
- 上传图片（PNG/JPG/扫描件）同流程
- 识别结果里的单词可直接点击，复用划词浮窗逻辑
- 两种模式：**快速翻译**（整图译文）/ **精细对照**（逐行双语）

### 2.3 文件上传翻译 + 总结

- 支持格式：PDF、DOCX、PPTX、TXT、Markdown、EPUB、图片
- 本地解析并保留结构（标题/段落/表格/公式）→ **左右双语对照视图**，段落级对齐、可折叠
- 一键总结：生成**摘要卡片**（3–5 句）、大纲、核心术语表、重点难点句；可导出 Markdown/DOCX/PDF
- 自动检测扫描版 PDF → 转图片走 OCR 管线
- 每段可单独点击"深入讲解"（语法结构/逻辑/背景知识）

### 2.4 AI 学术助手

- 聊天自动携带当前选中段落/文件上下文，可直接问"这句的主语是谁""第 3 页的 v 是什么意思"
- 新增：**基于当前文件的问答**，回答标注出处页码/段落
- 快捷指令：`/总结` `/翻译` `/解释术语` `/推导公式` `/出题`
- 流式打字输出（打字机效果），首 token 延迟目标 < 800ms

### 2.5 底座

- 收藏单词自动入生词本，带原文语境句
- 内置 CS/EE/生物/经济等学科术语包，可导入自定义词表
- 历史记录（翻译/截图/文件操作全记录）
- 设置（快捷键、OCR 与 LLM 服务商、语言、主题）

---

## 3. 响应速度设计（分层路由）

| 内容类型 | 通道 | 目标延迟 |
|----------|------|----------|
| 单词 | 本地词典 + 轻量词典 API | < 300ms |
| 短句 | 轻量翻译 API | < 1s |
| 长文/讲解/总结 | LLM 流式输出 | 首 token < 800ms |
| 图片/截图 | OCR（云端并行） | < 2s |
| 文件解析 | 本地解析（pdf.js 等） | 100 页 < 1s |
| 重复内容 | 本地 LRU 缓存 | < 50ms |

**预取策略**：截图后立即 OCR + 预翻译，面板弹出时结果已就绪；划词防抖 200ms；流式增量渲染，绝不转圈等待。

---

## 4. 技术选型

| 层 | 选型 | 说明 |
|----|------|------|
| 壳 | Tauri 2（备选 Electron） | 启动快、体积小 |
| 前端 | React + Vite + Tailwind CSS + shadcn/ui | AI 生成成功率最高 |
| OCR | 云端 OCR（火山/百度，质量优先）+ tesseract.js 兜底 | 截图场景选云端保准确率 |
| PDF | pdf.js（文本层）+ 扫描件检测 | 扫描页转图走 OCR |
| DOCX/PPTX/EPUB | mammoth.js / 解包 XML / epub.js | 保留标题与段落结构 |
| 截图 | 先做"上传图片"MVP，后接 Tauri/Rust 截屏库（xcap） | Windows 无官方截屏 API |
| LLM | DeepSeek / 豆包 / Kimi（OpenAI 兼容 SDK） | 便宜、快、国内直连 |
| 存储 | SQLite | 生词本/历史/缓存 |

---

## 5. UI 设计规范（macOS HIG · Sequoia 质感）

### 5.1 设计原则

安静克制 · 亚克力毛玻璃材质 · Squircle 平滑曲率 · 8pt 网格（间距取 8 的倍数）

### 5.2 材质与深度

- **标题栏/侧边栏**：半透明亚克力 `backdrop-filter: blur(12px)` + `bg-white/80`，配 1px 微亮边框 `border border-black/[0.08]`
- **卡片**：`rounded-xl` ~ `rounded-2xl` 平滑曲率；双层柔和悬浮阴影 `0 20px 40px -15px rgba(0,0,0,0.12)` 叠加 `0 1px 2px rgba(0,0,0,0.05)`
- **浮窗**：悬浮层级最高阴影 + 最大圆角（16px）
- 深色模式：同结构黑色玻璃，自动跟随系统

### 5.3 配色（macOS Accent Blue 体系）

| 角色 | 色值 |
|------|------|
| 主强调色 | `#007AFF`（macOS System Blue） |
| Hover / Active | `#0062CC` / `#004FB3` |
| 浅色衬底 / Tag | `#F0F7FF` 或 `rgba(0,122,255,0.08)` |
| 主按钮 | `#007AFF` 胶囊圆角 + 微蓝发光 `0 1px 2px rgba(0,122,255,0.25)` |
| 文本 | `#1D1D1F` / `#6E6E73` / `#AEAEB2` |
| 背景 | `#F5F5F7`（浅色） |
| 语义色 | 收藏黄 `#FFD60A`、成功绿 `#30D158`（极克制） |

- 彻底移除通用绿/红/粗糙色；强调色占比 < 5%
- 浅色模式保证 WCAG AA 对比度

### 5.4 字体排版

- 字族：`-apple-system, BlinkMacSystemFont, "SF Pro Text", "PingFang SC", sans-serif`
- 层级：大标题 22/600 → 标题 17/600 → 正文 15/400 → 说明 13 → 辅助 11

### 5.5 控件规范

- **Segmented Control（分段控制器）**：模式切换（翻译/讲解/总结）使用白底微阴影滑块 + 平滑过渡动画
- 按钮：胶囊圆角，Focus 环形高亮 `ring-2 ring-[#007AFF]/40`，Hover/Active/按下微交互顺滑
- 文件解析进度：细线型进度条，不打断阅读

### 5.6 关键界面

**① 划词浮窗**（320×160，亚克力材质，轻触外部消失）

```
┌──────────────────────────────┐
│  term              [术语] [☆]  │
│  n. 术语; 学期; 条件           │
│  例句（灰色小字）               │
│  ──────────────────────────   │
│  [ 翻译 ]  [ 讲解 ]  [ 收藏 ]   │  ← 胶囊小按钮
└──────────────────────────────┘
```

**② 主窗口**：左侧 86px 图标侧栏（生词本/历史/设置）+ 中央内容区 + 右侧可拖宽 320px AI 助手；默认 1280×800，无边框毛玻璃标题栏

**③ 文件导入视图**：中央 Squircle 虚线拖放区 → 文件列表（类型图标+大小+状态）→ Segmented Control `[翻译 | 总结 | 问答]` → 双语对照 / 摘要卡片

**④ 截图结果视图**：顶部操作条（复制/导出/收藏/讲解）+ 左右逐行对照（原图缩略+识别原文 | 译文）

### 5.7 动效

浮窗 200ms ease-out 淡入+上浮 4px；分段滑块 250ms spring；消息 250ms 淡入；按钮 0.15s 按压缩放（scale 0.97）；仅用 transform/opacity，保证 60fps

---

## 6. 开发路线图

| 里程碑 | 内容 | 验收标准 |
|--------|------|----------|
| **M0 骨架** | Tauri 项目 + 三栏布局 + 按 5.x 规范建 design tokens（CSS 变量 + tailwind.config） | 深/浅色模式切换正常，质感达标 |
| **M1 文件翻译** | 解析 PDF/DOCX/TXT → 流式双语对照 + 摘要卡片 | 100 页论文 10s 内出摘要 |
| **M2 划词浮窗** | 全局快捷键 + 剪贴板取词 → 毛玻璃浮窗 | Word/PDF 里可用 |
| **M3 AI 对话** | 上下文聊天 + 基于文件问答 + `/` 指令 | 能问"第 3 页的 v 是什么意思" |
| **M4 图片/截图翻译** | 先上传图片 OCR → 再全局截图 | 截图到译文 < 3s |
| **M5 打磨** | 生词本/历史/术语库/导出/打包 | 安装包 < 15MB |

---

## 7. Vibe Coding 实操建议

1. **先建 design tokens 文件**：把 5.2–5.5 全部写成 `tailwind.config.js` + CSS 变量，一次建好全局复用，之后所有组件自动苹果风
2. **接口先 Mock**：`services/ocr.ts`、`services/fileParser.ts`、`services/llm.ts` 定义好接口用假数据，API Key 最后填
3. **截图功能分两步**：先"上传图片"，跑通 OCR 管线后再做全局截图（Windows 截屏较复杂）
4. **一次一个小切片**：每个里程碑开新对话，先静态页面 + 假数据，跑通再接 API
5. **把附录提示词直接喂给工具**，并声明"严格遵循 tokens，不改业务逻辑"

---

## 附录：macOS 风 UI 重构提示词模板（直接复制给 vibe coding 工具）

```markdown
# Role & Goal
你是一位精通 Apple HIG 的高级前端 UI/UX 专家。请重构指定组件为 macOS
Sequoia/Sonoma 原生桌面质感，并调整主题色彩体系。

# Redesign Objectives
1. 视觉风格（macOS HIG）：
   - 毛玻璃材质：标题栏/侧边栏 backdrop-filter: blur(12px) + bg-white/80，
     配 1px 微亮边框 border border-black/[0.08]
   - 卡片：Squircle 圆角 rounded-xl ~ rounded-2xl，
     双层阴影 0 20px 40px -15px rgba(0,0,0,0.12)
   - 模式切换/Tab：macOS 标准 Segmented Control（白底微阴影滑块+平滑动画）
   - 字体：-apple-system, BlinkMacSystemFont, "SF Pro Text",
     "PingFang SC", sans-serif
2. 配色：
   - 主强调色 #007AFF；Hover #0062CC；Active #004FB3
   - 浅色衬底/Tag：#F0F7FF 或 rgba(0,122,255,0.08)
   - 主按钮 #007AFF 胶囊 + 微蓝发光 0 1px 2px rgba(0,122,255,0.25)
   - 移除通用绿/红/粗糙色
3. 约束：
   - 保持业务逻辑与 Props 接口不变，仅改样式/JSX 结构
   - 微交互（Hover/Active/Focus 环形高亮）顺滑自然
   - 深/浅色自适应且高对比度可读

# Target Components
- 组件路径：[填写]
- 待替换旧色：[旧色，如 #10b981 → #007AFF]

请分析组件结构，输出重构后的完整代码及关键 CSS 变更说明。
```
