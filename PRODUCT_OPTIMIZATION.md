# Academic Lens 产品优化方案

**版本**：v2.0（基于 MVP 代码评审 + 产品定位「英文阅读翻译伴侣」）
**范围**：双语对照视图 · 翻译核心能力增强 · 语音听觉层 · 原生集成 · 底座打磨
**原则**：贴近 macOS 原生体验（HIG/Sequoia 质感）、按价值/成本排序、分阶段落地

---

## 1. 现状体检（制约体验的 7 个点）

| # | 问题 | 位置 | 影响 |
|---|------|------|------|
| 1 | `Segment` 只有 `h/p` 两型，无表格/列表/代码/图片/公式块 | `src/lib/types.ts` | 无法保留格式对照 |
| 2 | DOCX 表格被 `cells.join(' \| ')` 拍平 | `src/lib/parse.ts:156-159` | 表格对照形同虚设 |
| 3 | PDF 解析丢弃页码信息 | `src/lib/parse.ts:91-135` | AI 助手"第 3 页的 v"无从对应 |
| 4 | 原文/译文同卡纵向堆叠，无左右对照、无滚动同步 | `src/components/FileView.tsx:113-170` | 不满足对照阅读诉求 |
| 5 | 每段一次独立 LLM 请求（并发 3），无批处理、无缓存 | `src/stores/fileStore.ts:63-125` | 慢且贵 |
| 6 | 导出只有 Markdown（`.md` 过滤器） | `electron/main.ts:93-102` | 交付格式单一 |
| 7 | 划词取词、OCR、朗读均未实现（设计文档 M2/M4 未落地） | — | 读扫描件/图片/听读场景空缺 |

---

## 2. 核心新板块：双语对照阅读视图

**目标**：左侧原始文本/文件，右侧中文译文，格式（标题/列表/表格/加粗/代码/图片/公式）完全保留、仅文字内容变化，行级对齐、滚动同步、hover 联动。

### 2.1 数据模型升级（地基，第一步）

```ts
// src/lib/types.ts
type Inline = { text: string; bold?: boolean; italic?: boolean; code?: boolean; link?: string }

type Block =
  | { kind: 'heading'; level: 1|2|3|4|5|6; runs: Inline[] }
  | { kind: 'paragraph'; runs: Inline[] }
  | { kind: 'list'; ordered: boolean; items: { runs: Inline[] }[] }
  | { kind: 'table'; rows: Inline[][][] }
  | { kind: 'code'; lang?: string; text: string }
  | { kind: 'blockquote'; runs: Inline[] }
  | { kind: 'image'; name: string; caption?: string }   // 两侧同图占位
  | { kind: 'math'; latex: string }                     // 原样保留，不翻译

interface Segment {
  id: string
  page?: number          // PDF 页码，支撑"第 3 页"引用（chatStore 扩展 @p3）
  block: Block
  translation: string    // 译文（含同样的行内标记 ** * `）
  translating: boolean
  error?: string
}
```

要点：
- 译文用**行内标记**保持格式，渲染时同一套小解析器把两侧都解析为 `Inline[]`。
- 图片/公式只存在于原文侧，译文侧渲染同一占位——"格式不变、仅文字变"的定义。
- `page` 字段同时解锁 QA 的页码级引用。

### 2.2 解析层改造

| 格式 | 做法 |
|------|------|
| DOCX | `mammoth.convertToHtml` 后按块重建：`p/h1-6/ul/ol/table/img/blockquote/code` 各成一块；表格保留行列；`<strong>/<em>/<code>/<a>` 提取为 `Inline[]` |
| PDF | 复用 `extractPdfLines` 启发式；追加列表前缀检测（`•`/`1.`）、`\|` 列对齐 → 表格块、每段记录 `page` |
| MD/TXT | 直接走 `marked.lexer` 的 AST（原生支持标题/列表/表格/代码/引用） |

### 2.3 翻译管线（快、稳、省）

- **批处理 + JSON 模式**：短段落按 8~12 段一组请求，系统提示固定输出 `{"1": "译文", ...}`；`electron/llm.ts` 增加 `response_format: { type: 'json_object' }`；按返回 key 精确回填，映射不错位。
- **长段落**单独流式请求（保留现有 `translateOne` 体验）。
- **表格**独立提示词："保持行列数与结构，只翻译文本，公式/数字/引用不动"。
- **术语一致性**：生词本/术语库注入系统提示："以下术语必须统一译法：`attention mechanism` → `注意力机制`…"。
- **本地缓存**：`hash(原文 + 术语版本 + model)` → 译文存 electron store，重开文档秒出。

### 2.4 双栏渲染（macOS 质感核心）

```
┌ 工具栏 ───────────────────────────────────────────────┐
│ [☰大纲] [逐段|逐句] [同步滚动◉] [A- A+] [遮译文] [导出] │
├ 大纲(可折叠) ───────┬──────────────────────────────────┤
│ ·1 Introduction    │ 左栏 原文            │ 右栏 译文  │
│ ·2 Method          │ 行级 grid 对齐        │ 行级对齐   │
│   ·2.1 …           │ hover 联动高亮        │ 同步滚动   │
└────────────────────┴──────────────────────────────────┘
```

- **左右两栏 + 可拖分隔条**：4px 分隔条 hover 变蓝、双击还原 50/50；默认**比例同步**滚动（防回环锁），可关。
- **行级配对**：一个块一行 grid（左原文 | 右译文），hover 任一格 → 另一侧 `ring` 微光联动；点击 → 双栏置顶定位。
- **大纲侧栏**（Preview.app 风格）：heading 树 + 页码，点击双栏联动滚动。
- **逐句对齐**：段落内用 `Intl.Segmenter('en', { granularity: 'sentence' })` 拆句，实现句子级双语对照（沉浸式翻译体验）。
- **遮译学习模式**：译文默认隐藏为占位横线，hover/点击显示。
- **术语高亮**：命中词表的词在译文栏高亮 + hover 出释义 tooltip。
- **虚拟化**：>300 块启用行级 windowing，保证 60fps。

### 2.5 导出升级（`electron/main.ts:93` 扩展 filters）

| 格式 | 做法 |
|------|------|
| 对照 MD | 每块一个两列表格 |
| 对照 DOCX | `docx` npm 包生成双列表格（Word/Pages 可开） |
| 对照 PDF | 隐藏 BrowserWindow + `webContents.printToPDF` + 双栏打印样式 |
| 双语 PDF（原排版） | `pdf-lib` 用 pdfjs 坐标在原文块下方插入译文，保留原排版 |
| 双语 EPUB | 块模型 → 双列 xhtml → zip 打包（无新依赖） |
| HTML 分享 | 单文件内嵌样式，可直接发人 |

---

## 3. 翻译核心能力增强

| 功能 | 说明 | 成本 |
|------|------|------|
| 批量文件翻译 | 一次拖入多文件，队列处理 + 进度/失败重试状态机 | 小 |
| 翻译记忆库 (TM) | 句子 hash → 译文表，跨文档复用，同类课件二次打开零成本 | 中 |
| 领域/风格预设 | 计算机论文/生物课件/新闻/口语，切换翻译提示词模板 | 小 |
| 逐词注释 (Gloss) | 选中句子 → 每词头上标词义，学英语神器 | 中 |
| 回译校验 | 译文回译对比，标记可疑句子（"质量检查"按钮） | 小 |
| 中→英反向翻译 | 中文论文/摘要 → 学术英语 + 润色建议（学术写作刚需） | 小 |
| 中英混合输出 | 译文内保留核心术语（注意力机制 (attention mechanism)） | 小 |
| 术语一致性检查报告 | 扫描同一英文词多种译法 → 告警列表（本地正则即可） | 小 |

---

## 4. 语音听觉层（macOS 零成本，性价比最高）

- **单词/句子发音**：调用系统 `say`（macOS）/ PowerShell SAPI（Win），译文侧每行 🔊 按钮。
- **整篇朗读 / 跟读模式**：`say -r 160` 可调语速，朗读高亮当前句 + 双栏同步滚动。
- **音色选择**：`say -v '?'` 枚举系统音色。

> 约 30 分钟即可上线，观感最"原生"的功能。

---

## 5. 交互与原生集成层

| 功能 | 说明 | 成本 |
|------|------|------|
| 划词浮窗 | 全局快捷键取词 → 亚克力浮窗（单词=音标+释义+例句；短语=整句译文+讲解），`[翻译][讲解][收藏]` | 中 |
| 剪贴板翻译 | 任意 App 复制即译（PopClip 式），托盘/菜单栏常驻 | 中 |
| macOS Services | 右键"用 Academic Lens 翻译"，注册 `NSServices` + URL scheme | 小 |
| Dock 拖入 | 文件拖到 Dock 图标直接打开翻译 | 小 |
| OCR 扫描件 | 文本层过薄检测 → tesseract.js 兜底 + 云端 OCR；截图翻译走 `screencapture -i -x` | 中/大 |
| 快捷键矩阵 | `Cmd+Shift+X` 立即翻译所选、`Cmd+Shift+C` 复制译文、`Cmd+1..4` 视图切换、`Cmd+F` 文档内查找 | 小 |

---

## 6. 底座与原生感打磨

| 项 | 做法 |
|----|------|
| 系统强调色 | macOS 用 `systemPreferences.getAccentColor()` 读取系统强调色替换 `#007AFF` |
| 原生菜单 | `Cmd+,` 设置、`Cmd+W`、`Cmd+R` 重试、`app.addRecentDocument` 最近打开 |
| 右键上下文菜单 | 原生 `Menu.buildFromTemplate`：复制/翻译此段/收藏/问 AI |
| 窗口记忆 | 尺寸/位置/双栏比例/面板开关持久化（用现有 store） |
| 生词本增强 | 记录词在文档首次出现位置、原文栏高亮词表词、导出 Anki CSV |
| 重试机制 | 失败段就地"重试"；批量断点续跑（跳过已译段） |
| Token 统计 | 上抛 `electron/llm.ts` 已收集的 `usage`，设置页显示用量与费用估算 |
| 历史 | 记录到文件哈希级，一键重开上次会话 |

---

## 7. 技术建议

- `patchSeg` 每次 set 全量数组：长文档（>2000 段）需 per-block 更新 + memo 子组件。
- 翻译并发提升意义不大，优先批处理（省 3~5 倍请求数）。
- 对照导出统一走块模型序列化，避免 `buildExportMd` 与新导出两套逻辑。
- 术语库/生词本/翻译缓存共用一套存储层（SQLite 或现有 electron store）。

---

## 8. 分阶段路线图

| 阶段 | 内容 | 验收标准 |
|------|------|----------|
| **P0 对照视图** | 块模型升级 → 解析器（PDF/DOCX/MD）→ 双栏渲染 + 滚动同步 + 行高亮 → 批处理 JSON 翻译 + 缓存 → 导出 MD/DOCX/PDF | 带表格/列表/加粗的 DOCX 对照后左右格式一致、仅文字变化；100 页论文翻译用时减半 |
| **P1 翻译能力** | 逐句对齐 · 批量文件 · 领域预设 · 朗读发音 · 剪贴板翻译 · 中→英反向翻译 · 逐词注释 · 划词浮窗 | 划词 300ms 出结果；朗读一键可用 |
| **P2 原生体验** | 系统强调色 · 原生菜单/右键 · 大纲侧栏 · 遮译模式 · `@页码` 引用 · 双语 PDF/EPUB 导出 · 术语一致性报告 · Token 统计 | 菜单快捷键全通；导出格式 ≥4 种 |
| **P3 能力补齐** | OCR 扫描件 · 截图翻译 · 回译校验 · Services/Dock 集成 · Anki 导出 · 多标签页 | 扫描版 PDF 走通"解析→对照"闭环 |

---

## 9. 优先级总表（快速启动项）

| 优先级 | 功能 | 预计投入 |
|--------|------|----------|
| 立即 | 朗读发音（say）· 领域预设 · 重试按钮 · Token 统计 | 各 ≤0.5 天 |
| 第一批 | 块模型 + 解析器 + 双栏对照 + 批处理翻译 + 缓存 | 5~8 天 |
| 第二批 | 导出矩阵（DOCX/PDF/EPUB）· 批量文件 · 逐句对齐 · 系统强调色 | 3~5 天 |
| 第三批 | 划词浮窗 · 剪贴板翻译 · 逐词注释 · 中→英 · OCR | 5~10 天 |
| 后续 | Services/Dock 集成 · 回译校验 · 多标签 · 生词本高亮 | 按需 |

---

**启动建议**：从 P0 的「块模型升级」开始——它是所有对照功能的地基；模型与解析器就绪后，双栏视图可直接替换现有 `BilingualView` 渲染层，风险最小。
