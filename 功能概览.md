# Academic Lens 功能概览

> 学术透镜 — 安静、快速、聪明的英文阅读伴侣
> Electron + Vite + React + TypeScript + Zustand

本文档汇总当前项目已实现的功能模块、核心实现路径与相关源码位置，供开发与维护参考。

---

## 一、三类输入：文档 / 文本 / 图片翻译

统一入口在 [HomeView.tsx](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/components/HomeView.tsx)，支持拖放、点击选择、Dock/Finder 打开文件并自动进入翻译。

### 1. 文档翻译（PDF / DOCX / TXT / MD）
- 解析：`lib/parse.ts` 调用 `pdfjs-dist`（PDF）、`mammoth`（DOCX），TXT/MD 直接切片
- 逐段对齐：英文原文 ↔ 中文译文对照阅读，支持单段/批量翻译、进度显示
- 视图：[FileView.tsx](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/components/FileView.tsx)
- 块模型保留格式：DOCX 表格真实行列、列表序号、加粗/斜体/代码/链接内联样式；PDF 列表（`•`/`1.`）与表格（`\|` 对齐）启发式识别；MD 用 `marked.lexer` AST 保留标题/列表/表格/代码/引用（详见下方 ***双语对照阅读视图（块模型）***）

### 2. 文本翻译
- 纯文本粘贴即译，自动分流：英文句子→中译、中文→英译
- 视图：[TextTranslateView.tsx](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/components/TextTranslateView.tsx)

### 3. 图片 / 截图翻译（OCR）
- `lib/ocr.ts` 基于 `tesseract.js`，支持英文 / 简体中文 / 双语（`eng+chi_sim`）
- 截图后 `Ctrl/Cmd+V` 自动识别，逐行对齐后进入翻译
- 视图：[ImageZoneView.tsx](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/components/ImageZoneView.tsx)

> 这些翻译能力也可从 **智能体对话框底部「#」按钮** 直达：上传文档 / 图片 → 解析或 OCR → 自动跳转到上方对应翻译视图（详见[四、对话「#」上传 → 原生翻译]）。

---

## 一·附加  双语对照阅读视图（块模型）

**数据模型**在 [types.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/lib/types.ts)：`Segment` 新增 `block: Block` 联合类型 + 行内 `Inline[]`（加粗/斜体/代码/链接），共 8 种块：

- `heading` 标题 / `paragraph` 段落 / `list` 列表（有序/无序）/ `table` 表格
- `code` 代码 / `blockquote` 引用 / `image` 图片占位 / `math` 公式（原样保留、不翻译）

> 保留 `type` / `text` 平铺投影作兼容层，既有提示词 / 历史 / 生词分析等消费点零改动（`blockText()` 由块推导可读文本）。

**解析层**（[parse.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/lib/parse.ts)）：

| 格式 | 改造 |
|------|------|
| DOCX | 表格保留真实行列（不再 `cells.join('|')` 拍平）；列表保留序号/圆点；`<strong>/<em>/<code>/<a>` → `Inline[]` |
| PDF | 新增列表前缀（`•`/`1.`）与 `\|` 列对齐表格检测 → 表格块；每段保留 `page` 页码 |
| MD/TXT | MD 用 `marked.lexer` AST 原生保留标题/列表/表格/代码/引用 |

**翻译管线**（[prompt.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/lib/prompt.ts) + [fileStore.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/stores/fileStore.ts)）：
- **术语一致性注入**：生词本词条（word → 中文释义，≤40 条）注入系统提示，术语全文译法统一（设置页开关）
- **领域/风格预设**：通用 / 计算机论文 / 生物医学 / 新闻时政 / 学术润色，切换翻译措辞（设置页选择）
- **表格独立提示词**：表格段走专门提示词（保持行列与 `|` 结构、数字/公式/引用不动），且不并入批处理、独立流式

**渲染**（[FileView.tsx](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/components/FileView.tsx) + [inline.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/lib/inline.ts)）：
- 按块渲染真实结构：表格 → `<table>`、列表 → 有序/无序列表、代码 → `<pre>`、图片/公式 → 占位、引用 → 引用样式
- 译文同样支持轻量行内标记（`**加粗**` / `*斜体*` / `` `代码` ``），表格译文按 `|` 还原为表格
- **逐句对齐**：`Intl.Segmenter` 按句子拆段为双语行，工具栏「逐段/逐句」切换

**导出**（[exportText.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/lib/exportText.ts)）：工具栏新增格式下拉——译文 MD / 双语对照 MD（每块一个两列表格）/ 双语对照 DOCX（`docx` 包生成双列表格，经 `file:saveBuffer` 写二进制）。

---

## 二、查词双轨（uapis 词典 + LLM）

核心分流在 [quickTranslate.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/lib/quickTranslate.ts)，设置见 [settingsStore.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/stores/settingsStore.ts)。

- **词典优先（dict）**：启用时纯英文单词先查 `uapis.cn` 真实词条（RAG/Grounding 层）
  - 成功 → 返回结构化词卡（音标 / 义项 / 例句 / 词形 / 真实发音）
  - 404 → 明确未收录 → 友好「未找到」引导卡片（可一键改用 AI 查词）
  - 服务不可用 → 自动回退 LLM
- **仅 LLM（llm）**：保持原位行为，不引入额外复核
- 客户端：[dictLookup.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/lib/dictLookup.ts)
- 词卡格式化与解析：[wordCard.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/lib/wordCard.ts)
- UI 呈现：[QuickTranslate.tsx](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/components/QuickTranslate.tsx)

**提速入口**：小窗模式 + `Ctrl/Cmd+Shift+T` 唤起，选中文字 `Ctrl/Cmd+X` 一键翻译；纯字母输入触发智能联想补全（`lib/suggest.ts`）。

---

## 三、生词本与智能整理

- 存储：[wordbookStore.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/stores/wordbookStore.ts)
- 视图：[WordbookView.tsx](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/components/WordbookView.tsx)

整理逻辑在 [organize.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/lib/organize.ts)：

### 普通整理（本地算法，零成本）
- **文本相似度分组**：Levenshtein 编辑距离 + 词形归一化（去屈折后缀），把同一词的不同形态归组
- **词性分组**：按 n. / v. / adj. 分类

### 智能整理（AI）
四种模式，严格 JSON 输出，只用词表内单词、每词唯一归组：
- **近反义词**：含义相近/相反归组并说明差异
- **专业 · 日常**：区分学术术语与日常通用词（可细分领域）
- **词根词缀**：按构词法归组并标注词根词缀含义
- **主题场景**：AI 自由归纳语义场

---

## 四、AI 对话 + 内置智能体（Harness）

对话页 [AgentView.tsx](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/components/AgentView.tsx)，协调层 [agentStore.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/stores/agentStore.ts)，harness 在 [agentTools.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/lib/agentTools.ts)。

### 架构
- **低级 / 轻量编排**：内置智能体接入 GLM-4-flash（免费），`lib/llm.ts` 的 `agentComplete` / `agentStream`
- **高级 / 重型任务**：翻译、摘要等用主 API（DeepSeek / MiMo / 豆包 / Kimi / 自定义 OpenAI 兼容）

### ReAct 多步循环（真 Agent）
协调层 [agentStore.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/stores/agentStore.ts) 现已内置 **ReAct（Reason + Act）循环**，见 [agentLoop.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/lib/agentLoop.ts)：

- **快路径（`resolveByRules`）**：一张确定性离线规则表先行匹配（零延迟、零成本），覆盖导航 / 生词本（概览·到期·列表·添加）/ 查词 / 分级 / 整理 / 抽卡 / 周报 / 文档 / 查词方式 / 个性化目标 / 朗读，命中即 await 单个工具并组织成一句话；否则进入 ReAct。
- **ReAct 主循环（`runReAct`）**：`思考 → 调用工具 → 结果回填为新的观察 → 再决策` 的多步编排，支持「先查词、再存入生词本」这类需要串联多个工具的需求；每步经 GLM 输出严格 JSON（`{tool,params}` 或 `{done,answer}`），有 `maxSteps` 上限防失控，`onStep` 把每步工具结果推给 UI。
- **重型工具（可 await）**：`heavyWordLookup` / `heavyGrade` / `heavyOrganize` / `heavyWordbookAdd` / `heavyDocExport` 在循环内异步执行、返回真实结果文本，而非一次性占位。
- **查词双轨回归**：dict 模式未配置 uapis Key 时明确弹 Notice 提示「本次回退 AI」，不再误导用户以为用了词典。

### 请求能力调优（上下文 / 回复长度）
- **上下文窗口**：全链路新增 `contextLength` 参数（`bridge/types.ts` → `electron/llm.ts` 与 `browser.ts` 透传为 `context_length`）；智能体路径（`agentComplete` / `agentStream`）默认声明 `context_length: 65536` 激活大上下文；多轮注入窗口 [agentStore.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/stores/agentStore.ts) 的 `CONTEXT_WINDOW` 提升至 24 条。
- **回复长度（maxTokens）**：ReAct 决策 400→1024、快路径组织话术（`agentStore` 流式）800→1400、AI 词卡 300→900、意图路由 [agentTools.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/lib/agentTools.ts) 250→512。
- **性能取舍**：决策层保持 `temperature: 0` 收紧；结构化解析（意图 / 词清洗）仍用小 maxTokens，只放大面向用户的「最终回答 / 词卡 / 组织话术」，避免决策卡顿。

### Harness 设计
- **工具白名单**：只允许名单内工具，默认只读，`sideEffect` 标记副作用
- **确定性意图映射**：先跑离线规则表（零延迟、零成本），模糊意图再交 GLM JSON 解析兜底
- **决策边界**：`maxParam` 参数上限 + 导航/操作白名单校验，越界即拒绝（`wordbook_add.maxParam` 为参数字符串总长度上限，调至 400）

**工具清单**：查词（双轨）、单词分级、整理生词、抽闪卡、文档上下文/摘要/生词命中/导出、学情周报、可信度核查、历史检索、设置学习目标、查看档案、跳转页面、切换查词方式、添加/概览/到期/列表生词、朗读、打开链接（完整清单见 [agentLoop.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/lib/agentLoop.ts) 的 `buildToolDescs`）。

### 词输入清洗与拼写纠错（离线优先 + LLM 兜底）
统一清洗入口 [wordClean.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/lib/wordClean.ts)，词库访问 [suggest.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/lib/suggest.ts)。小窗查词与智能体查词/存词共用同一套：

1. **离线正则**：剥音标（`/…/`、`[…]`）、标点、引号噪声，提取第一个字母 token 并统一小写。
2. **已知词短路（绝不改词）**：`isKnownWord` 二分校验词是否在 `public/words.txt`；已知词直接返回、不做任何纠错——因为该表是屈折/变体词表（含 `prudence` 却可能缺 base 词 `prudent`），自动替换会把合法词误改，故顺手误改一律禁止。
3. **纠错只在词典 miss 路径**：词典未收录时 `suggestSpelling` 先走离线 Levenshtein（`bestOfflineSpelling`，邻域 ±3000 窗口、置信门槛），离线不确定才交 GLM-4-flash 兜底。
4. **生词本前置校验（`heavyWordbookAdd`）**：添加生词前先清洗，再离线判错——若清洗词不在词库且有高置信的「更可能拼写」，**拦截并提示**，避免把错拼直接存进生词本；无高置信建议（专有名词 / 冷僻合法词）时才放行入库，避免误伤。

### 对话「#」上传 → 原生翻译
对话输入栏最左端提供 **「#」上传按钮**（[AgentView.tsx](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/components/AgentView.tsx) 的 `handleUpload`），可在对话框直接启动原有文档 / 图片翻译：

- **文档**（PDF / DOCX / TXT / MD）：走 `lib/parse.ts` 的 `parseAnyFile` → 载入 `fileStore.setDoc`
- **图片**（PNG / JPG / WebP / BMP / GIF）：走 `lib/ocr.ts` 的 `recognizeClipboardImage` OCR → 识别行转 Segment → `fileStore.setDoc`
- 成功载入后 `useAppStore.go('home')` **自动跳转到原生翻译视图**（FileView 逐段双语对照）；回到对话后，因 `getFileContextForChat` 每次实时读取当前文档，可直接追问「总结 / 翻译 / 生词命中」等，智能体自动携带新文档上下文
- 未知格式 / 空文档 / OCR 无文字时给出 toast 提醒并跳过，不跳转

> 说明：文件的选择与读取属前端能力（`window.bridge.openFiles` / `readFile`），LLM 无法直接存取文件，故「上传」由「#」按钮完成，解析与翻译复用既有 `parse` / `ocr` / `fileStore` 管线。

### 历史可还原 + 历史检索
解决"生成过摘要后无法还原、智能体也找不到先前译文"的痛点：

- **摘要全文入历史**：[fileStore.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/stores/fileStore.ts) 的 `summarize` 完成时把摘要正文写入 [historyStore.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/stores/historyStore.ts) 的 `HistoryEntry.payload`；[HistoryView.tsx](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/components/HistoryView.tsx) 支持展开查看 / 一键复制。
- **译文全文存档（Cap）**：整篇翻译全部完成后，[fileStore.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/stores/fileStore.ts) 把原文 + 译文写入独立 `docResults` 存储（保留最近 8 篇，超限清最旧）；存于 Electron `userData/data.json`（磁盘存储，无 localStorage 配额限制）。
- **历史检索工具**：新增 `history_search`（审查核实类），[historyStore.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/stores/historyStore.ts) 的 `searchRecords(keyword)` 按关键词（文档名等）检索历史摘要与文档译文并回填；[agentLoop.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/lib/agentLoop.ts) 提供 `heavyHistorySearch`（可 await）、[agentTools.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/lib/agentTools.ts) 的 `executeTool` 走异步回填，并新增一条自然语言快路径规则（如「独立宣言之前的译文」）。
- **对话直达**：[AgentView.tsx](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/components/AgentView.tsx) 的「+」技能面板会自动列出「历史检索」，点击填入示例指令。

---

## 五、闪卡学习（7 维词卡 + 复习 + 听写）

核心逻辑 [flashcard.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/lib/flashcard.ts)，存储 [flashcardStore.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/stores/flashcardStore.ts)，视图 [FlashcardView.tsx](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/components/FlashcardView.tsx)，调度 [srs.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/lib/srs.ts)（SM-2 风格间隔复习）。

- **7 维词卡增强**：音标 / 词性 / 释义 / 例句 / 常用搭配 / 语域 / 词族 / 内涵辨析 / 构词 / 关联词 + 难度分级
- **抽词**：正反面对应单词 / 含义 / 构词 / 关系，打通生词本收藏与 AI 生成
- **题型**：选择 / 填空 / 拼写 / 造句（AI 生成 + 造句 AI 批改：语法 / 搭配 / 词义用法三维打分）
- **听写模式**：正反面对应单词 / 含义 / 构词 / 关系
- **SRS 复习**：间隔复习 + `level` 掌握度回写

---

## 六、分级体系（CEFR + 中国考试）

分级逻辑 [levels.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/lib/levels.ts) + [flashcard.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/lib/flashcard.ts) 的 `aiGradeWords`。

- **CEFR**：A1 → C2
- **中国考纲**：四级 CET-4、六级 CET-6、专四 TEM-4、专八 TEM-8
- **出国考试**：雅思 5.5 / 7.0、托福 85 / 110

---

## 七、学情周报

生成在 [reportStore.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/stores/reportStore.ts)，视图 [StatsView.tsx](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/components/StatsView.tsx)，复习记录 [reviewLogStore.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/stores/reviewLogStore.ts)。

- 一屏读完的 Markdown 周报，不堆砌指标
- **本周概览**：复习量 / 正确率 / 新增 / 掌握 / 今日到期
- **薄弱主题**：聚合高频/易错主题给改进建议
- **易错词提醒**：遗忘最多词 + 记忆技巧
- **下周建议**：可执行建议

---

## 八、文档生词命中率 + 差分类比高亮

算法 [unknownWords.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/lib/unknownWords.ts)，视图 [FileView.tsx](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/components/FileView.tsx)。

- 统计文档中生词命中率（出现在生词本即视为"已认识"）
- 跳过常见高频词，只标记有学习价值的生词
- 生词在原文中按字符区间高亮定位，**只统计、不批量收藏**

---

## 九、美人美言专栏

存储 [quoteStore.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/stores/quoteStore.ts)，内容 [quotes.ts](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/lib/quotes.ts)，视图 [QuoteView.tsx](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/components/QuoteView.tsx)。

- 内置多条名人名言：英文原文 + 中文释义 + 出处链接
- 支持逐词查询释义（打通查词双轨）

---

## 十、设置中心（三栏 API + 偏好）

视图 [SettingsView.tsx](file:///c:/Users/-/Desktop/lib/python/academic-lens/src/components/SettingsView.tsx)。

- **翻译 / 重型任务（高配 API）**：DeepSeek / MiMo / 豆包 / Kimi / 自定义 OpenAI 兼容
- **智能体 / 轻量编排（低配 API）**：GLM-4-flash 免费，独立 baseUrl / key / model
- **词典查询（uapis.cn · 免费）**：API Key + 查词双轨选择（dict / llm）
- 主题（system / light / dark）、OCR 语言
- **翻译偏好**：领域/风格预设（通用 / 计算机论文 / 生物医学 / 新闻时政 / 学术润色）+ 术语一致性注入开关（生词本词条入翻译提示词）

---

## 十一、技术栈与构建

| 项 | 说明 |
| --- | --- |
| 框架 | Electron 43 + Vite 6 + React 19 |
| 语言 | TypeScript |
| 状态 | Zustand（分模块 store） |
| 样式 | Tailwind CSS 4（`src/styles/index.css`） |
| 文档导出 | `docx`（双语对照 DOCX） |
| 桥接 | `electron/main.ts` + `preload.ts` → `window.bridge`（IPC：`llm:complete` / `llm:stream` / `llm:cancel` / 文件 / 剪贴板 / 朗读 / 存储等） |
| 打包 | electron-builder，`npm run dist:win` / `dist:mac` |

**常用命令**
```bash
npm run dev       # 开发
npm run build     # 类型检查 + 构建
npm run smoke     # 构建 + 冒烟运行
npm run dist:win  # 打包 Windows 安装包
```